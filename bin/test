#!/usr/bin/env node

console.log('Running test...');

const userModel = require('../api/models/user');
const buisnessModel = require('../api/models/buisness');
const orderModel = require('../api/models/order');

const order = new orderModel('zack', ['test'], 'a', 'a', 'a', 'a', 'a', 'a');
console.log(order.serialize());